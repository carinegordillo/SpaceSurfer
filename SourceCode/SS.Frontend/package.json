{
  "name": "front-end",
  "version": "1.0.0",
  "main": "index.html",
  "scripts": {
    "build": "",
    "dev": "lite-server",
    "release": "npm run build & npm run test",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "devDependencies": {
      "lite-server": "^2.6.1"
    }
  }
}
